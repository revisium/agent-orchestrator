# Mirrors sonar-project.properties — keeps Automatic Analysis aligned with CI scanner.
sonar.exclusions=node_modules/**,dist/**,coverage/**,**/*.test.ts,package-lock.json
sonar.cpd.exclusions=**/*.test.ts,scripts/**
